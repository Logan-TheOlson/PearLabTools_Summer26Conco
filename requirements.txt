# Pear Tools — Python dependencies
# Install with:  pip install -r requirements.txt

# Runtime
matplotlib==3.10.9
pandas==3.0.3
pillow==12.2.0
tkinterdnd2==0.4.4.1

# Build only (packaging the standalone Windows .exe)
pyinstaller==6.20.0
